# Artifacts

Artifacts are the files that get copied, adapted, or installed.

## Split
- `artifacts/infrastructure/` → shared stack templates and conventions
- `artifacts/agents/` → one package per agent
- `artifacts/shared-skills/` → reusable skill folders
- `artifacts/schemas/` → structural data schemas
- `artifacts/knowledge-base-templates/` → starter knowledge-base templates
