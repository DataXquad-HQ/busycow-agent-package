# Collaboration Layer

V1 is collaboration-surface first, with Lark/Feishu as the current default path in this package.

Installers should decide:
- which channels receive human-readable ops summaries
- which channels receive raw machine logs
- which channels are used for approval requests
