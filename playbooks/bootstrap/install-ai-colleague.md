# Legacy Compatibility Wrapper — Install AI Colleague

This file is kept only for backward compatibility with older package references.

## Current canonical path
Use the new agent flow instead:
1. confirm `playbooks/infrastructure/04-verify-infrastructure.md` has passed
2. open `playbooks/agents/README.md`
3. run the specific agent install playbook, for example `playbooks/agents/install-iris.md`

Do not extend this legacy wrapper with new installation logic.
