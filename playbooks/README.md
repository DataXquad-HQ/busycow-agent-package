# Playbooks

Playbooks are **agent-readable instructions**.

They are split into two setup classes:
- `infrastructure/` — shared stack first
- `agents/` — install one agent package after infrastructure is ready

## Entry points
- `infrastructure/README.md`
- `agents/README.md`
